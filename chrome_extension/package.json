{
  "name": "durin-chrome-extension",
  "private": true,
  "description": "Vendored ONNX Runtime for the Durin tab blocker",
  "type": "module",
  "scripts": {
    "copy-model": "mkdir -p model && cp -R ../onnx_browser_model/. model/",
    "copy-ort": "mkdir -p lib && cp node_modules/onnxruntime-web/dist/ort.wasm.min.mjs lib/ && cp node_modules/onnxruntime-web/dist/ort-wasm-simd-threaded.wasm lib/ && cp node_modules/onnxruntime-web/dist/ort-wasm-simd-threaded.mjs lib/"
  },
  "dependencies": {
    "onnxruntime-web": "^1.23.0"
  }
}
